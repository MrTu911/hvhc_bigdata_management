/**
 * Re-export wrapper — backward compatibility.
 * Canonical implementation đã được merge vào lib/security/rate-limiter.ts.
 * Không import trực tiếp file này trong code mới — dùng lib/security/rate-limiter.ts.
 */
export * from '@/lib/security/rate-limiter';
