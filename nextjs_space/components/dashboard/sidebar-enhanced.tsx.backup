
'use client';

import Link from 'next/link';
import { usePathname } from 'next/navigation';
import { cn } from '@/lib/utils';
import { useLanguage } from '@/components/providers/language-provider';
import { useState } from 'react';
import {
  LayoutDashboard,
  Users,
  Server,
  Activity,
  AlertCircle,
  FileText,
  Settings,
  Upload,
  Search,
  BarChart3,
  Database,
  Brain,
  Cpu,
  Shield,
  ScrollText,
  Lock,
  CheckCircle,
  Clock,
  GraduationCap,
  FlaskConical,
  ChevronDown,
  ChevronRight,
  Bell,
} from 'lucide-react';

interface NavItem {
  name: string;
  href?: string;
  icon: any;
  children?: NavItem[];
  badge?: string;
  gradient?: string;
}

const navigationGroups = [
  {
    title: 'nav.overview',
    items: [
      {
        name: 'nav.dashboard',
        href: '/dashboard',
        icon: LayoutDashboard,
        gradient: 'from-blue-500 to-blue-600',
      },
    ],
  },
  {
    title: 'nav.coreModules',
    items: [
      {
        name: 'nav.datalake',
        href: '/dashboard/datalake',
        icon: Database,
        gradient: 'from-cyan-500 to-cyan-600',
      },
      {
        name: 'nav.training',
        href: '/dashboard/training',
        icon: GraduationCap,
        gradient: 'from-purple-500 to-purple-600',
      },
      {
        name: 'nav.research',
        href: '/dashboard/research',
        icon: FlaskConical,
        gradient: 'from-pink-500 to-pink-600',
      },
      {
        name: 'nav.aiTraining',
        href: '/dashboard/ai-training',
        icon: Brain,
        gradient: 'from-indigo-500 to-indigo-600',
      },
    ],
  },
  {
    title: 'nav.dataManagement',
    items: [
      {
        name: 'nav.data',
        icon: Database,
        gradient: 'from-emerald-500 to-emerald-600',
        children: [
          {
            name: 'nav.upload',
            href: '/dashboard/data/upload',
            icon: Upload,
          },
          {
            name: 'nav.query',
            href: '/dashboard/data/query',
            icon: Search,
          },
          {
            name: 'nav.preview',
            href: '/data/preview',
            icon: FileText,
          },
          {
            name: 'nav.files',
            href: '/dashboard/files',
            icon: Upload,
          },
        ],
      },
      {
        name: 'nav.analytics',
        icon: BarChart3,
        gradient: 'from-orange-500 to-orange-600',
        children: [
          {
            name: 'nav.basicAnalytics',
            href: '/dashboard/analytics',
            icon: BarChart3,
          },
          {
            name: 'nav.advancedAnalytics',
            href: '/dashboard/analytics/advanced',
            icon: BarChart3,
          },
        ],
      },
      {
        name: 'nav.reports',
        href: '/dashboard/reports',
        icon: FileText,
        gradient: 'from-amber-500 to-amber-600',
      },
    ],
  },
  {
    title: 'nav.mlEngine',
    items: [
      {
        name: 'nav.ml',
        icon: Brain,
        gradient: 'from-violet-500 to-violet-600',
        children: [
          {
            name: 'nav.models',
            href: '/dashboard/ml/models',
            icon: Brain,
          },
          {
            name: 'nav.training',
            href: '/dashboard/ml/training',
            icon: Cpu,
          },
          {
            name: 'nav.tracking',
            href: '/dashboard/ml/tracking',
            icon: Activity,
          },
          {
            name: 'nav.experiments',
            href: '/dashboard/ml/experiments',
            icon: Activity,
          },
          {
            name: 'nav.versions',
            href: '/dashboard/ml/versions',
            icon: CheckCircle,
          },
          {
            name: 'nav.workflows',
            href: '/dashboard/ml/workflows',
            icon: Server,
          },
        ],
      },
    ],
  },
  {
    title: 'nav.systemAdmin',
    items: [
      {
        name: 'nav.users',
        href: '/dashboard/users',
        icon: Users,
        gradient: 'from-teal-500 to-teal-600',
      },
      {
        name: 'nav.services',
        href: '/dashboard/services',
        icon: Server,
        gradient: 'from-slate-500 to-slate-600',
      },
      {
        name: 'nav.monitoring',
        icon: Activity,
        gradient: 'from-green-500 to-green-600',
        children: [
          {
            name: 'nav.systemMonitoring',
            href: '/dashboard/monitoring',
            icon: Activity,
          },
          {
            name: 'nav.realtimeMonitoring',
            href: '/dashboard/realtime',
            icon: Activity,
          },
        ],
      },
      {
        name: 'nav.alerts',
        href: '/dashboard/alerts',
        icon: AlertCircle,
        gradient: 'from-red-500 to-red-600',
        badge: '3',
      },
      {
        name: 'nav.notifications',
        href: '/dashboard/notifications/history',
        icon: Bell,
        gradient: 'from-indigo-500 to-indigo-600',
      },
    ],
  },
  {
    title: 'nav.securityGovernance',
    items: [
      {
        name: 'nav.security',
        icon: Shield,
        gradient: 'from-rose-500 to-rose-600',
        children: [
          {
            name: 'nav.audit',
            href: '/dashboard/security/audit',
            icon: Shield,
          },
          {
            name: 'nav.loginAudit',
            href: '/dashboard/security/login-audit',
            icon: Shield,
          },
          {
            name: 'nav.permissions',
            href: '/dashboard/security/permissions',
            icon: Lock,
          },
          {
            name: 'nav.policy',
            href: '/dashboard/security/policy',
            icon: Shield,
          },
        ],
      },
      {
        name: 'nav.logs',
        href: '/dashboard/logs',
        icon: ScrollText,
        gradient: 'from-gray-500 to-gray-600',
      },
      {
        name: 'nav.governance',
        icon: FileText,
        gradient: 'from-blue-500 to-blue-600',
        children: [
          {
            name: 'nav.compliance',
            href: '/dashboard/governance/compliance',
            icon: CheckCircle,
          },
          {
            name: 'nav.retention',
            href: '/dashboard/governance/retention',
            icon: Clock,
          },
        ],
      },
    ],
  },
  {
    title: 'nav.configuration',
    items: [
      {
        name: 'nav.settings',
        icon: Settings,
        gradient: 'from-zinc-500 to-zinc-600',
        children: [
          {
            name: 'nav.appearance',
            href: '/settings/appearance',
            icon: Settings,
          },
          {
            name: 'nav.generalSettings',
            href: '/dashboard/settings',
            icon: Settings,
          },
        ],
      },
    ],
  },
];

export function DashboardSidebarEnhanced() {
  const pathname = usePathname();
  const { t } = useLanguage();
  const [openMenus, setOpenMenus] = useState<string[]>([]);

  const toggleMenu = (name: string) => {
    setOpenMenus(prev =>
      prev.includes(name)
        ? prev.filter(item => item !== name)
        : [...prev, name]
    );
  };

  const renderNavItem = (item: NavItem, isChild = false) => {
    const Icon = item.icon;
    const hasChildren = item.children && item.children.length > 0;
    const isOpen = openMenus.includes(item.name);
    const isActive = pathname === item.href;

    if (hasChildren) {
      return (
        <div key={item.name} className="space-y-1">
          <button
            onClick={() => toggleMenu(item.name)}
            className={cn(
              'w-full flex items-center justify-between gap-3 rounded-lg px-3 py-2.5 text-sm font-medium transition-all duration-200',
              isChild
                ? 'text-muted-foreground hover:bg-muted/50 hover:text-foreground'
                : 'text-foreground hover:bg-gradient-to-r hover:from-primary/10 hover:to-primary/5'
            )}
          >
            <div className="flex items-center gap-3">
              <div className={cn(
                'h-8 w-8 rounded-lg flex items-center justify-center transition-all',
                !isChild && item.gradient && `bg-gradient-to-br ${item.gradient} text-white shadow-md`
              )}>
                <Icon className="h-4 w-4" />
              </div>
              <span>{t(item.name)}</span>
            </div>
            {isOpen ? (
              <ChevronDown className="h-4 w-4 transition-transform" />
            ) : (
              <ChevronRight className="h-4 w-4 transition-transform" />
            )}
          </button>
          {isOpen && (
            <div className="ml-11 space-y-1 animate-in slide-in-from-left duration-200">
              {item.children?.map(child => renderNavItem(child, true))}
            </div>
          )}
        </div>
      );
    }

    return (
      <Link
        key={item.href}
        href={item.href || '#'}
        className={cn(
          'flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm font-medium transition-all duration-200',
          'hover:scale-105',
          isActive
            ? item.gradient 
              ? `bg-gradient-to-r ${item.gradient} text-white shadow-lg shadow-primary/20`
              : 'bg-primary text-primary-foreground shadow-lg'
            : isChild
              ? 'text-muted-foreground hover:bg-muted/50 hover:text-foreground'
              : 'text-foreground hover:bg-gradient-to-r hover:from-primary/10 hover:to-primary/5'
        )}
      >
        <div className={cn(
          'h-8 w-8 rounded-lg flex items-center justify-center transition-all',
          isActive && !isChild && item.gradient
            ? 'bg-white/20 shadow-inner'
            : !isChild && item.gradient
              ? `bg-gradient-to-br ${item.gradient} text-white shadow-md`
              : ''
        )}>
          <Icon className="h-4 w-4" />
        </div>
        <span className="flex-1">{t(item.name)}</span>
        {item.badge && (
          <span className="h-5 w-5 rounded-full bg-red-500 text-white text-xs flex items-center justify-center font-bold animate-pulse">
            {item.badge}
          </span>
        )}
      </Link>
    );
  };

  return (
    <aside className="hidden lg:fixed lg:inset-y-0 lg:left-0 lg:z-40 lg:block lg:w-64 lg:overflow-y-auto lg:border-r lg:pt-16 lg:bg-gradient-to-b from-background to-muted/20 backdrop-blur-xl">
      <nav className="flex flex-col gap-6 p-4">
        {navigationGroups.map(group => (
          <div key={group.title} className="space-y-2">
            <h3 className="px-3 text-xs font-semibold uppercase tracking-wider text-muted-foreground/70">
              {t(group.title)}
            </h3>
            <div className="space-y-1">
              {group.items.map(item => renderNavItem(item))}
            </div>
          </div>
        ))}
      </nav>
    </aside>
  );
}
